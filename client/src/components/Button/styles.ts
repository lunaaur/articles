import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: "60%",
  },
  text: {
    color: "white",
    fontWeight: "600",
    textTransform: "uppercase",
  },
  button: {
    borderRadius: 10,
    backgroundColor: "#6757ff",
    width: "40%",
    height: 40,
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
});

export default styles;
